#include <x86/Types.h>
#include <x86/Logger.h>
#include <x86/Console.h>

/*
void  bmemcpy(void* o1, void* o2,uint32_t length){

	
	uint8_t* from = static_cast<uint8_t*>(o1);
	uint8_t* to= static_cast <uint8_t*>(o2);
//	debugString("\nlength for memcpy: ");debugLong(length);
	
	for(uint32_t i=0;i<length;i++)
	{
		to[i]= from[i];
	}

}*/
